declaration:
  call: declare
  version: 0.1
  name: "Current Electricity Price"
  description: "This service fetches electricity prices for the current hour. The output should be in the format of 'Current elecrtricity price is (result) kWh'"
  method: "GET"
  accepts: JSON
  returns: JSON
  namespace: common-services
  allowlist:
    body:
      - field: chatId
        type: string
        description: "The chat ID for the message"
      - field: authorId
        type: string
        description: "The author ID for the message"
  

initialize:
  assign:
    apiUrl: https://dashboard.elering.ee/api/nps/price/EE/current
  next: makeApiCall

makeApiCall:
  call: http.get
  args:
    url: ${apiUrl}
  result: apiResponse
  next: checkResponse

checkResponse:
  switch:
    - condition: ${apiResponse.response.statusCodeValue >= 400}
      next: errorResponse
  next: assignResponse

errorResponse:
  assign:
    res:
      result: "API call failed"
  next: formatMessage

assignResponse:
  assign:
    res: ${apiResponse.response.body}
  next: formatMessage

formatMessage:
  assign:
    res:
      result: "Current electricity price is ${res} kWh"

formatMessages:
  call: http.post
  args:
    url: "[#DMAPPER]/common-services/hbs/bot_responses_to_messages"
    headers:
      type: json
    body:
      data:
        {
          "botMessages": "${[res]}",
          "chatId": "${chatId}",
          "authorId": "${authorId}",
          "authorFirstName": "",
          "authorLastName": "",
          "authorTimestamp": "${new Date().toISOString()}",
          "created": "${new Date().toISOString()}"
        }
  result: formatMessage

return_result:
  return: ${formatMessage.response.body}
